/*Debugging quiz - File No: 1 */
/* The program should print the sum of all even numbers between 1 to 100 
Identify each error and fix it */

#include<stdio.h>

int main(void){
	int limit = 100, sum;
	for(int counter = 0; counter < limit; counter+2 = counter){
		sum = +counter;
	printf("Sum = %d\n", counter);
	}
